Put 25 test-split images here (one per class, 25 distinct classes, none used in
train/val) for the latency benchmark screen — see `paper/benchmark_latencia/PROTOCOLO.md`,
step 2. Name them by class, e.g. `ceviche.jpg`, `lomo_saltado.jpg`.

Then list them in `kAssets` in `lib/features/benchmark/benchmark_screen.dart`.

This README is just a placeholder so the `assets/bench/` directory declared in
pubspec.yaml exists before the real images are added; delete it once real images are here.
